<%= form_with(model: toodoo_item, local: true) do |form| %>
  <% if toodoo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(toodoo_item.errors.count, "error") %> prohibited this toodoo_item from being saved:</h2>

      <ul>
      <% toodoo_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
